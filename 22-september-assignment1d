#include<stdio.h>
void reverse(int n)
    {
    int rev=0,r;
    while(n!=0)
    {
        r=n%10;
        rev=(rev*10) + r;
        n=n/10;
    }
    printf("%d",rev);
    }
int main()
{
    int n;
    scanf("%d",&n);
    reverse(n);
    return 0;
}
